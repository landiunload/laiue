# Память проекта laiue

## Предпочтения по общению
- Все ответы ассистента — на русском языке.

## О проекте
Воксельный движок на C под Windows без CRT (D3D12). Модульная сборка
через CMake (`laiue_add_module`). Подробнее — в README.md.
