#pragma once

#include "physics/rigid_body.h"

#include <stdbool.h>
#include <stdint.h>

/*
 * Build-time слияние compound-коробок.
 *
 * Функция схлопывает набор неперекрывающихся axis-aligned коробок в
 * минимальный по жадному правилу набор гране-смежных коробок, не меняя
 * объединение геометрии. Это инструмент сборки формы (например, из блочных
 * примитивов), а не шаг симуляции: он не выделяет память, не трогает ОС и
 * детерминирован. Материал и масса входа не участвуют — слияние чисто
 * геометрическое, поэтому массу и полный тензор инерции приложение считает
 * по исходным детям, а не по результату.
 *
 * Контракт:
 *  - boxes — count элементов, каждый непустой (положительные конечные
 *    полуребра), с конечными невырожденными границами center +- halfExtent.
 *  - Коробки обязаны быть строго неперекрывающимися: касание гранями
 *    перекрытием не считается и разрешено.
 *  - capacity >= count и capacity >= 1; outCount <= count на успехе.
 *  - boxes == outBoxes разрешено ровно для полного совмещения (in-place).
 *    Частично перекрывающиеся входной и выходной диапазоны, а также outCount,
 *    указывающий в любой из буферов, отклоняются.
 *  - При любом отказе выходной буфер и *outCount побитово не меняются.
 *  - FP-режим нормализуется через VoxelPhysicsConfigureThread до арифметики,
 *    поэтому результат не зависит от враждебного MXCSR/FPCR.
 *
 * Алгоритм: повторные проходы по осям 0,1,2 в стабильном порядке пар
 * (i < j, остаётся меньший индекс, удалённый компактится). Пара сливается
 * только если по двум другим осям обе границы совпадают точно и по
 * выбранной оси грани смыкаются точно. Никаких допусков, заполнения зазоров
 * и дырок. Новые center/halfExtent обязаны точно восстановить объединённые
 * концы граней; иначе пара пропускается (представимость на огромных
 * координатах). Геометрия каждого слияния — точное объединение двух коробок,
 * поэтому объединение всего набора сохраняется побитово.
 *
 * Возвращает true и записывает *outCount, если вход корректен. Если слияний
 * нет, layout входа остаётся без изменений (outputCount == count). Возвращает
 * false без записи при неверных указателях, count, capacity, нефинитных или
 * вырожденных границах, перекрытии коробок или запрещённом aliasing.
 */
LAIUE_PHYSICS_API bool VoxelRigidCompoundMergeBoxes(const VoxelRigidCompoundBox *boxes,
                                                    uint32_t count,
                                                    VoxelRigidCompoundBox *outBoxes,
                                                    uint32_t capacity, uint32_t *outCount);
