# Сводный отчёт по оптимизациям движка

Состояние: исходный интеграционный кандидат на базе `wip/providers-2026-09-23` и `codex/architecture-tradeoffs` уже вошёл в `main`; обе ветки Dependabot также учтены в его истории. Ниже сохранена исходная оценка, а в конце добавлено измерение последующей оптимизации стриминга.

## Что вошло в кандидат

- Перенос модулей и provider-интерфейсов из `wip/providers-2026-09-23`.
- Оптимизации раундов 1–2 по world/physics, audio, render, content, media, numeric, modding и task pool; вместе с ними — регрессионные тесты и ручные A/B-бенчмарки.
- Раунд 3: хранение backend в заголовке объекта рендера, компактный порядок загрузки модов и однопроходное перечисление контент-каталога через компактный staging-буфер.
- Ветка Dependabot `actions/checkout` объединена; в актуальном workflow уже используется тот же SHA `3d3c42e5…` для v7.0.1.
- Ветка Dependabot `actions/download-artifact` объединена в истории. Её workflow основан на устаревшей структуре CI; актуальный workflow не скачивает artifacts, поэтому SHA v8.0.1 там пока негде использовать. Старые jobs из этой ветки не возвращались.

## Измерения оптимизаций

Ниже отдельно отмечены замеры авторов и повторный A/B-прогон в интеграции. Benchmark мира пришлось связать с numeric provider: после переноса сервисной архитектуры standalone-стенд создавал мир без numeric-сервиса, молча не выполнял операции и печатал нулевой checksum. Стенд теперь устанавливает сервис явно, считает ошибки ошибкой процесса и печатает ненулевые контрольные суммы.

| Изменение | Результат замера | Цена или оговорка |
|---|---|---|
| `WorldTrySetBlock` и пакетные изменения | **Повторный замер:** одиночный сценарий: WIP **761.7 мс**, кандидат **254.8 мс** (−66.5% времени); пакет 4096 изменений в 4 чанках: **37.58 → 2.26 мс** (−94.0%); в 4096 чанках: **176.42 → 4.53 мс** (−97.4%). | Ранее записанное авторами замедление одиночной записи на 18.58% на этой интегрированной сборке не воспроизвелось. Повтор: MSVC Release, Windows x64, три запуска на вариант, в каждом запуске benchmark берёт медиану пяти измерений. Оба сценария дали ненулевые совпадающие checksums. |
| Компактный буфер перечисления каталога | Пиковый commit процесса ниже на 28.7–29.0%; сценарии с 2048/4096 файлами быстрее на 2.5–8%; суммарное время набора сценариев ниже на 3.1–5.2% в 8 из 9 пар | Для каталога из 64 файлов перечисление медленнее на 1.2–4.0%, или 8–27 мкс; этот короткий сценарий остаётся локальной ценой оптимизации. |
| Заголовок backend в объекте Renderer | `RendererGetBackend` быстрее на 17–82% в 9/9 парах; примерно 280 B меньше глобальных данных | Каждый Renderer стал на 4 B больше; кадровые стадии были в шуме замера. |
| Порядок загруженных модов | `GetLoaded` быстрее примерно в 32 раза; объект mod-host меньше на 976 B | Замер регистрации сервисов менял знак между базами из-за размещения кода в DLL; этот результат нестабилен. |

Для compact-catalog автор сравнивал с `9f1e16c`; его цифры не воспроизводились в этой интеграции. Мировой A/B сравнивал непосредственно `wip/providers-2026-09-23` и интеграционного кандидата на одной машине. A/B-стенды сохранены в `benchmarks/`.

## Интеграция и исправления

- Git обнаружил девять содержательных конфликтов. В разрешении сохранены provider-границы `wip`: аудиомодуль получает контент только через таблицу сервиса, renderer оставляет provider-файлы и переносит `renderer_internal.h` в новое расположение, world batch освобождает память через allocator самого мира.
- Переписаны устаревшие пути к исходникам в CMake после переезда `src/render`, `src/physics` и `src/media`.
- DDA- и UI-тесты подключены к новым публичным интерфейсам. Тест звукового PCM16 обращается к загрузчику через таблицу сервиса; тесты world, panorama и texture pack явно устанавливают необходимые providers.
- Исправлена проверка optional graphics module: headless-тест теперь выбирает offscreen Vulkan, а не оконный D3D12 через AUTO.
- При слиянии веток Dependabot сохранён актуальный workflow из интегрируемой архитектурной ветки, а не возвращены старые jobs упаковки, которые расходятся с новым набором CMake presets. Используемый checkout SHA уже совпадал с версией v7.0.1 из Dependabot; `download-artifact` в актуальном workflow не вызывается.

## Проверки интеграционного кандидата

- Visual Studio 2026, Windows x64, MSVC; CMake preset `windows-msvc`.
- Release: сборка прошла, CTest **71/71**.
- Debug: сборка прошла, CTest **71/71**.
- Проверка архитектурных границ прошла в обеих конфигурациях.
- Полный CI для GitHub Actions здесь не запускался; в workflow нет конфликтных маркеров, локальная C/C++ матрица прошла.
- Standalone world benchmark собран и полностью запущен после исправления provider setup; все пять сценариев прошли, checksums ненулевые.

## Решение перед публикацией

Оптимизацию мира оставил: после восстановления корректной работы benchmark provider setup замедление одиночной записи не подтвердилось, а повторные измерения показали ускорение одиночного сценария примерно на 66.5% и пакетных сценариев на 94.0–97.4%. Компактный каталог тоже оставлен: он уменьшает пик памяти примерно на 29% и сокращает общее время набора сценариев в 8 из 9 пар; цена — лишь 8–27 мкс на перечислении очень маленького каталога из 64 файлов. Release/Debug CTest кандидата прошли 71/71.

## Последующее измерение: очередь стриминга чанков

- `QueueChunkIfMissing` теперь объединяет поиск и вставку в один проход открытой адресации. Если запись уже есть, она возвращается; если ключа нет, первый пустой слот сразу заполняется. Раньше сначала выполнялся `FindEntry`, а затем отдельный `InsertEntry`, то есть отсутствующий чанк проходил цепочку пробирования дважды.
- A/B сравнение против `HEAD eebc212` выполнено на Windows x64/MSVC Release. Для каждого варианта запущено по три полных прохода standalone `laiue_streaming_benchmark`, радиус 12, 200 шагов; сравнивается медиана времени `SetCenter` на вызов.

| Сценарий | База, мкс/вызов | Изменение, мкс/вызов | Снижение времени |
|---|---:|---:|---:|
| worker sleep 0 мс | 83.599 | 73.399 | 12.2% |
| worker sleep 8 мс | 155.300 | 139.800 | 10.0% |

Это выигрыш именно в микробенчмарке смены центра, а не измерение FPS игры. Для этой правки расход памяти отдельно не замерялся; алгоритм не добавляет таблиц или постоянного состояния. Добавлен тест повторной установки того же центра: он проверяет, что заявки и эпоха центра не меняются; тест стриминга прошёл в Release и Debug.

## Раунд 4: оптимизации DeepSeek V4.1 Flash, 2026-09-27

Шесть отдельных задач OpenCode проверяли Vulkan submit, обновление мира, audio mixer, реестр модулей, chunk mesher и rigid-body solver. После них DeepSeek V4.1 Flash отдельно выполнил read-only аудит полного diff. В `main` вошли только измеренные или нужные для корректного измерения изменения.

| Изменение | Результат A/B | Цена и границы |
|---|---|---|
| Кэш активного `VkPipeline` в командном буфере | CPU-время отправки draw-команд: `draw_mesh` −63.8%, `draw_inst1` −73.4%, `draw_inst32` −73.6%; checksum одинаковый | GPU-время не измеряется этим стендом. +16 байт на Renderer. Кэш сбрасывается при начале буфера и смене/пересоздании pipeline. Прямого отдельного теста wireframe-переключения нет; Vulkan lifecycle и offscreen-тесты прошли. |
| Повторное использование хеша в `WorldTrySetBlock` | dense update −24.2%, sparse update −14.8%, toggle −9.0%; checksum одинаковый | Память мира не меняется; DLL выросла примерно на 1 KiB. Дополнительно сохранён пустой no-op fast path: запись базового блока в мире без правок возвращается до вычисления хеша. |
| Ограничение обхода аудиослотов последним используемым индексом | Для одно-голосовых сценариев mixer стал быстрее на 5.3–28.9%; большие наборы голосов в пределах шума (+0.8–2.2%) | Размер `AudioDevice` при замере не изменился, постоянной памяти не добавилось. Сканирующая граница принадлежит потоку вывода и не уменьшается после остановки всех голосов, поэтому после использования высокого слота обход остаётся до него. |
| Предварительный FNV-хеш имени сервиса в module host | Lookup: −11.7%…−85.2%; регистрация 128 сервисов −45.2%; lifecycle −12.1%…−19.7%; checksum одинаковый | До +1 KiB на host с полным реестром из 128 записей. Публичный ABI не меняется. Имя и таблица опубликованного сервиса должны оставаться валидными и неизменными до снятия регистрации; контракт теперь документирован. |
| Исправление benchmark setup | World и rigid standalone-бенчмарки теперь ставят numeric provider; mesher benchmark тоже ставит provider и хеширует содержимое квадов. Раньше mesher мог измерять пустой чанк: `quads=0`, около 0.0025 ms | Исправлена достоверность замеров, это не production-оптимизация. Добавлен benchmark module host для воспроизводимого измерения реестра. |

A/B-цифры сняты агентами в Windows x64 Release на MSVC 19.51; использовались попеременные прогоны baseline/candidate и медианы. Отдельные smoke-запуски интегрированной сборки подтвердили ненулевые checksum: world dense `14a3a01fffd26e80`, world sparse `7a5d7830700ad680`, mesher mixed — 104238 квадов, hash `5266760844138573104`. Новый module-host benchmark теперь вычисляет измерение и checksum последовательно; это исправило дефект порядка вычисления аргументов, пойманный независимым аудитом. Ревью не нашло других actionable-дефектов в библиотечном коде или остальных benchmark-изменениях.

### Кандидаты, которые не перенесены

- Увеличение lookahead rigid solver с 8 до 32 ухудшило медиану примерно на 0.36% для solver и на 0.74% для полного шага; production-константа оставлена прежней.
- Эксперименты с production chunk mesher не дали устойчивого выигрыша, поэтому изменён только harness.
- Alpha fast path в resampler из отдельной старой ветки дал в сумме сценариев лишь около 1.05% выигрыша, но замедлил zero-alpha сценарии на 7.1–7.6% и один sparse-alpha сценарий на 4.9%; он отклонён, поскольку результат заметно зависит от распределения альфы и памяти не экономит.

### Проверки интеграции

- Visual Studio 2026 / MSVC, CMake preset `windows-msvc`: полная Release-сборка и CTest **71/71**; полная Debug-сборка и CTest **71/71**.
- Отдельно собраны mesher, world batch, module host и оба rigid-body benchmark-targets. Module-host, world dense/sparse, mesher mixed и оба rigid-body benchmark smoke-запуска завершились с кодом 0.
- `git diff --check` прошёл. GitHub CI будет проверен после отправки коммита.