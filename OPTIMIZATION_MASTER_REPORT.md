# Сводный отчёт по оптимизациям движка

Состояние: собран локальный интеграционный кандидат на базе `wip/providers-2026-09-23` и `codex/architecture-tradeoffs`; обе ветки Dependabot также включены в историю интеграции. Кандидат пока не отправлен в `main`.

## Что вошло в кандидат

- Перенос модулей и provider-интерфейсов из `wip/providers-2026-09-23`.
- Оптимизации раундов 1–2 по world/physics, audio, render, content, media, numeric, modding и task pool; вместе с ними — регрессионные тесты и ручные A/B-бенчмарки.
- Раунд 3: хранение backend в заголовке объекта рендера, компактный порядок загрузки модов и однопроходное перечисление контент-каталога через компактный staging-буфер.
- Ветка Dependabot `actions/checkout` объединена; в актуальном workflow уже используется тот же SHA `3d3c42e5…` для v7.0.1.
- Ветка Dependabot `actions/download-artifact` объединена в истории. Её workflow основан на устаревшей структуре CI; актуальный workflow не скачивает artifacts, поэтому SHA v8.0.1 там пока негде использовать. Старые jobs из этой ветки не возвращались.

## Измерения оптимизаций

Ниже отдельно отмечены замеры авторов и повторный A/B-прогон в интеграции. Benchmark мира пришлось связать с numeric provider: после переноса сервисной архитектуры standalone-стенд создавал мир без numeric-сервиса, молча не выполнял операции и печатал нулевой checksum. Стенд теперь устанавливает сервис явно, считает ошибки ошибкой процесса и печатает ненулевые контрольные суммы.

| Изменение | Результат замера | Цена или оговорка |
|---|---|---|
| `WorldTrySetBlock` и пакетные изменения | **Повторный замер:** одиночный сценарий: WIP **761.7 мс**, кандидат **254.8 мс** (−66.5% времени); пакет 4096 изменений в 4 чанках: **37.58 → 2.26 мс** (−94.0%); в 4096 чанках: **176.42 → 4.53 мс** (−97.4%). | Ранее записанное авторами замедление одиночной записи на 18.58% на этой интегрированной сборке не воспроизвелось. Повтор: MSVC Release, Windows x64, три запуска на вариант, в каждом запуске benchmark берёт медиану пяти измерений. Оба сценария дали ненулевые совпадающие checksums. |
| Компактный буфер перечисления каталога | Пиковый commit процесса ниже на 28.7–29.0%; сценарии с 2048/4096 файлами быстрее на 2.5–8%; суммарное время набора сценариев ниже на 3.1–5.2% в 8 из 9 пар | Для каталога из 64 файлов перечисление медленнее на 1.2–4.0%, или 8–27 мкс; этот короткий сценарий остаётся локальной ценой оптимизации. |
| Заголовок backend в объекте Renderer | `RendererGetBackend` быстрее на 17–82% в 9/9 парах; примерно 280 B меньше глобальных данных | Каждый Renderer стал на 4 B больше; кадровые стадии были в шуме замера. |
| Порядок загруженных модов | `GetLoaded` быстрее примерно в 32 раза; объект mod-host меньше на 976 B | Замер регистрации сервисов менял знак между базами из-за размещения кода в DLL; этот результат нестабилен. |

Для compact-catalog автор сравнивал с `9f1e16c`; его цифры не воспроизводились в этой интеграции. Мировой A/B сравнивал непосредственно `wip/providers-2026-09-23` и интеграционного кандидата на одной машине. A/B-стенды сохранены в `benchmarks/`.

## Интеграция и исправления

- Git обнаружил девять содержательных конфликтов. В разрешении сохранены provider-границы `wip`: аудиомодуль получает контент только через таблицу сервиса, renderer оставляет provider-файлы и переносит `renderer_internal.h` в новое расположение, world batch освобождает память через allocator самого мира.
- Переписаны устаревшие пути к исходникам в CMake после переезда `src/render`, `src/physics` и `src/media`.
- DDA- и UI-тесты подключены к новым публичным интерфейсам. Тест звукового PCM16 обращается к загрузчику через таблицу сервиса; тесты world, panorama и texture pack явно устанавливают необходимые providers.
- Исправлена проверка optional graphics module: headless-тест теперь выбирает offscreen Vulkan, а не оконный D3D12 через AUTO.
- При слиянии веток Dependabot сохранён актуальный workflow из интегрируемой архитектурной ветки, а не возвращены старые jobs упаковки, которые расходятся с новым набором CMake presets. Используемый checkout SHA уже совпадал с версией v7.0.1 из Dependabot; `download-artifact` в актуальном workflow не вызывается.

## Проверки интеграционного кандидата

- Visual Studio 2026, Windows x64, MSVC; CMake preset `windows-msvc`.
- Release: сборка прошла, CTest **71/71**.
- Debug: сборка прошла, CTest **71/71**.
- Проверка архитектурных границ прошла в обеих конфигурациях.
- Полный CI для GitHub Actions здесь не запускался; в workflow нет конфликтных маркеров, локальная C/C++ матрица прошла.
- Standalone world benchmark собран и полностью запущен после исправления provider setup; все пять сценариев прошли, checksums ненулевые.

## Решение перед публикацией

Оптимизацию мира оставил: после восстановления корректной работы benchmark provider setup замедление одиночной записи не подтвердилось, а повторные измерения показали ускорение одиночного сценария примерно на 66.5% и пакетных сценариев на 94.0–97.4%. Компактный каталог тоже оставлен: он уменьшает пик памяти примерно на 29% и сокращает общее время набора сценариев в 8 из 9 пар; цена — лишь 8–27 мкс на перечислении очень маленького каталога из 64 файлов. Release/Debug CTest кандидата прошли 71/71.
